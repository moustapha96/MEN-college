<form method="post" action="{{ path('client_rapport_delete', {'id': rapport.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport.id) }}">
	<button class="btn btn-danger  " style="color:white">
		<i class="fa fa-trash" aria-hidden="true"></i>
		Supprimer</button>
</form>
