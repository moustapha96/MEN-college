<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
		<meta http-equiv="x-ua-compatible" content="ie=edge">
		<title>{{getName()}}</title>
		<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

		<link rel="icon" type="image/png" href="	images/{{getLogo2()}}"/>


		<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.11.2/css/all.css">
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap">
		<link rel="stylesheet" href="login/css/mdb.min.css">
		<link rel="stylesheet" href="login/css/style.css">

		<style>
			body,
			html {
				margin: 0;
				padding: 0;
				height: 100%;
			}
			.center-container {
				display: flex;
				justify-content: center;
				align-items: center;
				min-height: 100vh;
				flex-wrap: wrap;
			}
			.centered-element {
				text-align: center;
				padding: 20px;
				border: 1px solid #ccc;
				border-radius: 5px;
				background-color: #f5f5f5;
				width: 100%;
				max-width: 700px;

			}
			.responsive-image {
				max-width: 100%;
				height: auto;
				border-radius: 3%;
			}
			.footer {
				text-align: center;
				padding: 2px;
				background-color: #f5f5f5;
				position: fixed;
				bottom: 0;
				width: 100%;
			}
		</style>
	</head>
	<body>
		<div class="center-container">
			<div class="">
				<form class="bg-white rounded shadow-5-strong p-5" action="{{ path('app_reset_password') }}" method="post">

					<div class="row mb-3  centered-element">

						<div class="row  d-flex justify-content-between ">
							<img src="images/{{getLogo2()}}" style="width: 100px;height: 50px " class="responsive-image" alt="">
							<img src="images/{{getLogo1()}}" style=" height: 55px; width: 150px" class="responsive-image" alt="">
						</div>
						<h5 class="mt-1 ">
							<b>
								{{getTitle1()}}
							</b>
						</h5>
						<h5 class="mt-2">
							<b>
								{{getTitle2()}}</b>
						</h5>
					</div>

					<div class="row">
						{% for label, messages in app.flashes %}
							{% for message in messages %}
								<div class="alert alert-{{ label }}">
									{{ message }}
								</div>
							{% endfor %}
						{% endfor %}
					</div>


					<h3>Nouveau mot de paase</h3>
					{% set tmp = app.session.remove('session_val') %}

					<label for="new_password">Nouveau Mot de Passe</label>
					<input type="password" id="new_password" name="new_password" required minlength="6" class="col-6 mx-auto form-control">

					<label for="confirm_password">Confirmer Mot de Passe</label>
					<input type="password" id="confirm_password" name="confirm_password" required minlength="6" class="col-6 mx-auto form-control">


					<div class="d-flex mt-3 justify-content-between">

						<button type="submit" class="btn btn-primary btn-block">
							Enregistrer</button>

						<a class="btn btn-outline-primary btn-block" href="{{ path('app_login') }}">se connecter</a>
					</div>


				</form>
			</div>
		</div>
		<!-- Footer -->
		<div class="footer">
			<p>&copy; 2023
				{{getName()}},
				<i class="fas fa-mail-bulk"></i>
				<a href="mailto:{{getEmail()}}">
					{{getEmail()}}
				</a>
				.
				<i class="fa fa-solid fa-phone"></i>
				TEL:
				{{getTel()}}</p>

		</div>

		<script type="text/javascript" src="login/js/mdb.min.js"></script>
		<script type="text/javascript" src="login/js/script.js"></script>
		<script>
			(function () {
'use strict'
const forms = document.querySelectorAll('.requires-validation')
Array.from(forms).forEach(function (form) {
form.addEventListener('submit', function (event) {
if (! form.checkValidity()) {
event.preventDefault()
event.stopPropagation()
}

form.classList.add('was-validated')
}, false)
})
})()
		</script>

	</body>
</html>
